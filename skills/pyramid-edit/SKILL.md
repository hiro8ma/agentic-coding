---
name: pyramid-edit
description: >
  文書の構成を、結論を先に置き根拠をその下にまとめる形（ピラミッド原則）に並べ替える。
  「構成を直して」「結論が先に来るように」「並べ替えて」「話の順番が分かりにくい」のように、
  文ではなく段落や節の順番を直したいときに使う。情報は削らず並べ替えだけを行う。
  文そのものの直しは japanese-tech-writing に任せる。
---

# ピラミッド原則で並べ替える

文書の中身は変えず、並びだけを「結論 → 根拠 → 詳細」に直す。

## 手順

1. 文書全体を読み、いちばん伝えたい結論を 1 文で書き出す
2. 結論を支える根拠を 2〜5 個に分け、それぞれにぶら下がる詳細を振り分ける
3. 結論、根拠、詳細の順に並べ替える。同じ階層の根拠は、同じ種類の分け方（時系列、重要度、構成要素など）にそろえる
4. 並べ替えた全文を見せ、確かめてもらってから上書きする

## 守ること

- 情報を落とさない。並べ替えるだけにし、削るのは明らかな重複だけにする
- 文字数が元の半分以下になったら、落とした情報がないかを点検する
- 上書きする前に、そのファイルが Git で追跡されていることを確かめる。追跡されていなければ、元の文書を残す
- 文の言い回しは直さない。直すなら `japanese-tech-writing` を使う
